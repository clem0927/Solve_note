name: Deploy to EC2

on:
  push:
    branches: [deploy]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Write SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_KEY }}" > ~/.ssh/key
          chmod 600 ~/.ssh/key

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key_path: ~/.ssh/key
          script: |
            cd ~/solvenote/spring_project
            git fetch origin
            git reset --hard origin/deploy

            cd ~/solvenote/Solvenote_Ai
            git fetch origin
            git reset --hard origin/main

            cd ~/solvenote
            docker compose up -d --build